# 复制为 .env 或在部署平台的环境变量里设置

# 必填:JWT 签名密钥,务必用随机长串,泄露=任何人可伪造登录
# 生成: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=请替换为随机64位十六进制串

# 本地 http 调试设 false;部署到 https 域名后设 true
SECURE_COOKIE=false
